{
  "name": "google-custom-metrics",
  "version": "0.0.0",
  "description": "upload stackdriver custom metrics to google stackdriver",
  "license": "Apache-2.0",
  "repository": "https://github.com/andrasq/node-google-custom-metrics/#readme",
  "keywords": [ "gcp", "stackdriver", "metrics", "custom metrics", "stats", "monitoring" ],
  "devDependencies": {
    "qnit": "0.18.2"
  },
  "scripts": {
    "test": "qnit test*",
    "coverage": "nyc --reporter lcov --reporter text npm test",
    "clean": "rm -rf .nyc_output coverage"
  }
}
